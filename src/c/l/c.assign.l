<*>"&="       { LEXTOK(AND_ASSIGN); }
<*>"+="       { LEXTOK(ADD_ASSIGN); }
<*>"-="       { LEXTOK(SUB_ASSIGN); }
<*>"*="       { LEXTOK(MUL_ASSIGN); }
<*>"%="       { LEXTOK(MOD_ASSIGN); }
<*>"|="       { LEXTOK(OR_ASSIGN); }
<*>"^="       { LEXTOK(XOR_ASSIGN); }
<*>"/="       { LEXTOK(DIV_ASSIGN); }
<*>"<<="      { LEXTOK(LEFT_ASSIGN); }
<*>">>="      { LEXTOK(RIGHT_ASSIGN); }

